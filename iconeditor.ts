namespace kojac {

    class IconEditor extends Picker {
        constructor(cursor: Cursor) {
            super(cursor)
            // make 5 x 5 matrix of buttons
        }

        public onButtonClicked(icon: string) {
            // toggle the button state
        }
    }
}